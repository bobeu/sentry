# Set these values in the deployment platform's secret manager. Do not commit a real .env.production file.
# Prisma Postgres example: get both generated URLs from Prisma Console > Connect.
DATABASE_URL="postgres://USER:PASSWORD@pooled.db.prisma.io:5432/postgres?sslmode=require"
DIRECT_URL="postgres://USER:PASSWORD@db.prisma.io:5432/postgres?sslmode=require"

# DATABASE_URL is for the running Next.js application and workers.
# DIRECT_URL is only for Prisma CLI migrations, introspection, and admin tooling.
# Add all non-database secrets listed in .env.example through the deployment secret manager.